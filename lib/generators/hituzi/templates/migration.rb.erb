class CreateHituziTables < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table 'hituzi_dictionaries', force: :cascade do |t|
      t.string :name, null: false
      t.text :textdata
      t.text :dict
    end
    add_index :hituzi_dictionaries, :name, unique: true
  end
end
